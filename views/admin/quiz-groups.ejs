<%- include ./header -%>


<div class="alert alert-info center-block" style="max-width: 90%">
    A <b>published</b> quiz is available for students in the tutorial to open and be assigned to a group automatically (you can assign/change groups manually at any time).
    An <b>active</b> quiz is one currently in progress: the questions are visible to students and they can submit responses while a quiz is active.
    Note that students will not be automatically assigned to groups while a quiz is active - you will have to do it manually.
</div>

<p>
    <label class="col control-label">
        <span>Publish?</span>
        <input type="checkbox" value="1" class="bootstrap-switch" id="publish"<%= 
            tutorialQuiz.published ? ' checked' : '' %><%= disable ? ' disabled' : '' %>>
    </label>
    <label class="col control-label">
        <span>Active?</span> 
        <input type="checkbox" value="1" class="bootstrap-switch" id="activate"<%= 
            tutorialQuiz.active ? ' checked' : '' %><%= disable ? ' disabled' : '' %>>
    </label>
    <label class="col control-label">
        <span>Archive?</span>
        <input type="checkbox" value="1" class="bootstrap-switch" id="archive"<%= 
            tutorialQuiz.archived ? ' checked' : '' %><%= disable ? ' disabled' : '' %>>
    </label>
</p>

<h1>Groups</h1>

<%- include ./messages %>

<p>
    <button type="button" class="btn btn-default btn-sm" id="btn-refresh-groups"<%= disable ? 
        ' disabled' : '' %>><span class="glyphicon glyphicon-refresh"></span> Refresh</button>
    <button type="button" class="btn btn-default btn-sm" id="btn-add-group"<%= disable ? 
        ' disabled' : '' %>><span class="glyphicon glyphicon-plus"></span> Add group</button>
    <button type="button" class="btn btn-default btn-sm btn-primary" id="btn-save-groups"<%= disable ? 
        ' disabled' : '' %>><span class="glyphicon glyphicon-ok"></span> Save groups</button>
</p>

<table id="table-quiz-groups">
    <thead>
        <tr>
            <th rowspan="2" class="s">&nbsp;</th>
            <th colspan="<%= 1 + groups.length %>">Groups</th>
        </tr>
        <% if (!disable) { %>
            <tr>
                <th class="u">Unassigned</th>
                <% _.each(groups, function (group) { %>
                    <th class="g"><%= group.name %></th>
                <% }) %>
            </tr>
        <% } %>
    </thead>
    <tbody>
    <% if (!disable) { %>
        <% _.each(tutorial.students, function (student) { %>
            <tr>
                <td class="s">
                    <a href="/admin/courses/<%= course.id %>/students/<%= student.id %>/marks"><%= student.name.full %></a>
                </td>
                <td class="u">
                    <input type="radio" name="groups[<%= student.id %>]" value="unassigned">
                </td>
                <% _.each(groups, function (group) { %>
                    <td class="g"><input type="radio" name="groups[<%= student.id %>]" value="<%= group.id %>"<%=
                        _.find(group.members, { id: student.id }) ? ' checked' : '' %>></td>
                <% }) %>
            </tr>
        <% }) %>
    <% } else { %>
        <tr>
            <td colspan="2">No students have been added into the tutorial yet. Click <a href="/admin/courses/<%= course.id %>/students">here</a> to add students into tutorial.</td>
        </tr>
    <% } %>
    </tbody>
</table>

<script type="text/template" id="panel-group-template">
    <div class="panel panel-default" data-group-id="new">
        <div class="panel-heading" data-parent="#panel-groups" data-target="#group-${ id }">
            <h4 class="panel-title">New group</h4>
            <a href="#" class="btn btn-defaut"><span class="glyphicon glyphicon-remove"></span> Delete Group</a>
        </div>
        <div id="group-${ id }" class="panel-collapse collapse in">
            <div class="panel-body sortable"></div>
        </div>
    </div>
</script>

<%- include ./body-js -%>

<script>
$(function () {
    var url = {
        publish: '/admin/courses/<%= course.id %>/tutorial-quizzes/<%= tutorialQuiz.id %>/publish',
        activate: '/admin/courses/<%= course.id %>/tutorial-quizzes/<%= tutorialQuiz.id %>/activate',
        archive: '/admin/courses/<%= course.id %>/tutorial-quizzes/<%= tutorialQuiz.id %>/archive',
        save: '/admin/courses/<%= course.id %>/tutorial-quizzes/<%= tutorialQuiz.id %>/groups'
    };
    // set option on quiz in tutorial
    $(':checkbox').on('switchChange.bootstrapSwitch', function (e, state) {
        $.ajax(url[this.id], {
            type: 'put',
            data: { state: state }, 
            success: function (res) {
                console.log(res.status);
            }
        });
    });
    // add new group to table
    $('#btn-add-group').click(function () {
        var $table = $('#table-quiz-groups'),
            $thead = $table.find('thead'),
            $tbody = $table.find('tbody');
        // get next group #
        var numbers = [];
        $thead.find('tr:eq(1) th').each(function () {
            if (!isNaN(this.innerText)) {
                numbers.push(parseInt(this.innerText, 10));
            }
        });
        var value = _.toInteger(_.max(numbers)) + 1;
        // increment colspan
        $thead.find('tr:eq(0) th:eq(1)').attr('colspan', function (i, attr) { 
            return parseInt(attr, 10) + 1; 
        });
        // add header column
        $thead.find('tr:eq(1)').append('<th class="g">' + value + '</th>');
        // add body column
        $tbody.find('tr').each(function () { 
            var $tr = $(this);
            var name = $tr.find(':radio:eq(0)').attr('name');
            $tr.append('<th class="g"><input type="radio" name="' + name + '" value="' + value + '"></th>');
        });
    });
    // save changes
    $('#btn-save-groups').click(function () {
        $.ajax(url.save, {
            type: 'put',
            data: $('#table-quiz-groups :input').serialize(), 
            success: function (res) {
                if (res.status) {
                    window.location.reload(true);
                }
            }
        });
    });
    // refresh table
    $('#btn-refresh-groups').click(function (e) {
        e.preventDefault();
        window.location.reload(true);
    });
    // mark students with no groups as unassigned
    $('#table-quiz-groups tbody tr:not(:has(:checked))').each(function () {
        $(this).find('.u :radio').prop('checked', true);
    });
});
</script>

<%- include ./footer -%>