var _ = require('underscore');

// models
var Group = require('../models/group');

// Retrieve list of groups for tutorial
GroupController.getGroups = function (req, res) { };

// Retrieve group for tutorial
GroupController.getGroup = function (req, res) { };

// Create new group; if student adds the group, then they are considered as leader of the group
GroupController.addGroup = function (req, res) { };

// Delete group
GroupController.deleteGroup= function (req, res) { };